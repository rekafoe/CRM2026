/**
 * Дубликат маршрута BePaid webhook — основной обработчик в routes/webhooks.ts.
 * Оставлен для совместимости модулей Telegram; делегирует туда же логику нельзя без циклических импортов,
 * поэтому файл реэкспортирует тот же router.
 */
export { default } from '../../../routes/webhooks'
