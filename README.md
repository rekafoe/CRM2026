# 🏢 CRM System - Полиграфия

Современная CRM система для управления заказами полиграфической компании.

## 🚀 Возможности

- 📋 **Управление заказами** - создание, редактирование, отслеживание статусов
- 🧮 **Калькулятор печати** - автоматический расчет стоимости заказов
- 📦 **Управление материалами** - контроль остатков и поставок
- 📊 **Отчеты и аналитика** - детальная статистика по заказам
- 👥 **Многопользовательская система** - роли и права доступа
- 📱 **Telegram бот** - заказы через мессенджер
- 🖼️ **Заказ фото** - обработка и печать фотографий

## 🛠️ Технологии

### Frontend
- React 18 + TypeScript
- Vite (сборка)
- CSS (без инлайн-стилей; стили вынесены в отдельные файлы)
- React Router

### Backend
- Node.js + Express
- TypeScript
- SQLite (база данных)
- Sharp (обработка изображений)
- Multer (загрузка файлов)

### Интеграции
- Telegram Bot API
- Vercel (frontend деплой)
- Railway (backend деплой)

## 📦 Установка

### Локальная разработка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/rekafoe/CRM2026.git
cd CRM2026
```

2. **Установите зависимости:**
```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

3. **Настройте переменные окружения:**
```bash
# Backend
cp backend/env.example backend/.env

# Frontend
cp frontend/env.example frontend/.env
```
Отредактируйте значения под своё окружение. Для продакшена обязательно задайте реальные URL, токены и чувствительные данные через переменные окружения CI/CD.

4. **Инициализация базы данных (локально):**
```bash
cd backend
npm run migrate:dev
```
Для наполнения демо-данными (статусы, пользователи, материалы) можно запустить:
  ```bash
  cd backend
  npm run seed:demo
  ```

5. **Запустите серверы:**
```bash
# Backend (порт 3001)
cd backend
npm run dev

# Frontend (порт 5173)
cd frontend
npm run dev
```

6. **Сборка проекта (production):**
```bash
# В корне
npm run build

# Миграции и запуск backend
cd backend
npm run migrate
npm start

# Frontend (статичная выдача)
cd ../frontend
npm run build
npm run preview -- --host 0.0.0.0 --port 4173
```

## 🌐 Деплой: Vercel (frontend) + Railway (backend)

### Frontend → Vercel
- **Root Directory**: `frontend`
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Environment Variables**:
  - `VITE_API_URL=https://<railway-backend-domain>/api`

### Backend → Railway
- **Root Directory**: `backend`
- **Deploy**: из `backend/Dockerfile` (Railway Docker build)
- **Environment Variables (минимум)**:
  - `NODE_ENV=production`
  - `JWT_SECRET=...`
  - `CORS_ORIGIN=https://<vercel-domain>`
  - `DB_FILE=/data/data.db` (рекомендуется использовать Railway Volume)
  - `PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium` (уже задано в Dockerfile)

### Миграции БД (Railway)
В `backend/Dockerfile` запуск уже делает миграции перед стартом: `npm run migrate && npm run start`.

### Docker
Backend:
```bash
cd backend
docker build -t crm-backend .
docker run --env-file .env -p 3001:3001 crm-backend
```

Frontend:
```bash
cd frontend
docker build -t crm-frontend .
docker run -p 5173:5173 crm-frontend
```

## 📱 Telegram Bot

Бот поддерживает:
- `/start` - приветствие
- `/order_photo` - заказ фотографий
- `/my_orders` - мои заказы
- `/stock` - проверка остатков
- `/stock_pdf` - отчет по остаткам

## 🗂️ Структура проекта

```
CRM/
├── backend/                 # Backend API
│   ├── src/
│   │   ├── controllers/     # Контроллеры
│   │   ├── services/        # Бизнес-логика
│   │   ├── models/          # Модели данных
│   │   ├── routes/          # API маршруты
│   │   └── migrations/      # Миграции БД
│   └── package.json
├── frontend/                # Frontend приложение
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── pages/           # Страницы
│   │   ├── hooks/           # React хуки
│   │   └── styles/          # Стили
│   └── package.json
├── shared/                  # Общие типы
└── frontend/vercel.json     # Конфигурация Vercel (SPA rewrites)
```

## 🔧 Разработка

### Добавление новых функций

1. **Backend:** Создайте контроллер, сервис, модель
2. **Frontend:** Создайте компонент, страницу
3. **Миграции:** Обновите схему БД
4. **Тестирование:** Проверьте функциональность

### Стиль кода

- Используйте TypeScript строго
- Следуйте Flux архитектуре
- Компоненты должны быть модульными
- Избегайте хардкода
- Выносите стили в отдельные файлы

## 🔒 Безопасность

Перед production-запуском:

- **Смените JWT_SECRET** в `.env` на криптостойкое значение
- **Настройте CORS_ORIGIN** на актуальный домен (не используйте `*`)
- **Проверьте зависимости**: `npm audit` в backend/frontend
- **Ограничьте rate-limit** для публичных endpoints
- **Включите HTTPS** на prod-сервере
- **Регулярно обновляйте зависимости** и патчи безопасности

## 📊 Мониторинг и логи

Настройка:
- **LOG_LEVEL**: DEBUG | INFO | WARN | ERROR (default: INFO в production)
- **SENTRY_DSN**: (опционально) для автоматического отслеживания ошибок
- **Health check**: `GET /health` возвращает статус сервера

В production логи пишутся в stdout/stderr; рекомендуется использовать агрегатор (CloudWatch, Datadog, Loki).

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли браузера
2. Проверьте логи сервера
3. Убедитесь в правильности переменных окружения
4. Создайте Issue в GitHub
