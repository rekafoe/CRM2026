# 🚀 Возможности оптимизации CRM системы

## Обзор
Документ описывает области для оптимизации производительности без изменения функционала.

---

## 1. ⚡ Кеширование проверок структуры таблиц (PRAGMA table_info)

### Проблема
В коде **53+ места**, где выполняется `PRAGMA table_info` для проверки наличия колонок. Это выполняется при каждом запросе, хотя структура таблицы меняется редко.

**Примеры:**
- `operationsController.ts` - 7 вызовов
- `products/routes/products.ts` - 4 вызова
- `orderService.ts` - множественные вызовы

### Решение
Создать кеширующий сервис для проверки колонок:

```typescript
// backend/src/utils/tableSchemaCache.ts
class TableSchemaCache {
  private static cache = new Map<string, Set<string>>();
  private static cacheTime = new Map<string, number>();
  private static TTL = 5 * 60 * 1000; // 5 минут

  static async hasColumn(table: string, column: string): Promise<boolean> {
    const columns = await this.getColumns(table);
    return columns.has(column);
  }

  static async getColumns(table: string): Promise<Set<string>> {
    const now = Date.now();
    const cached = this.cache.get(table);
    const cachedTime = this.cacheTime.get(table);

    if (cached && cachedTime && (now - cachedTime) < this.TTL) {
      return cached;
    }

    const db = await getDb();
    const rows = await db.all<Array<{ name: string }>>(`PRAGMA table_info(${table})`);
    const columns = new Set(rows.map(r => r.name));
    
    this.cache.set(table, columns);
    this.cacheTime.set(table, now);
    
    return columns;
  }

  static invalidate(table?: string) {
    if (table) {
      this.cache.delete(table);
      this.cacheTime.delete(table);
    } else {
      this.cache.clear();
      this.cacheTime.clear();
    }
  }
}
```

**Ожидаемый эффект:** Снижение количества запросов к БД на 50-80% в местах проверки колонок.

---

## 2. 🔄 Устранение N+1 проблемы в загрузке items

### Проблема
В `orderService.ts:867-907` загрузка items для каждого заказа выполняется в цикле:

```typescript
for (const order of orders) {
  const itemsRaw = await db.all('SELECT ... FROM items WHERE orderId = ?', order.id);
  // обработка...
}
```

При 100 заказах = 101 запрос (1 для заказов + 100 для items).

### Решение
Загрузить все items одним запросом:

```typescript
// После загрузки orders
const orderIds = orders.map(o => o.id);
const placeholders = orderIds.map(() => '?').join(',');

const allItems = await db.all(`
  SELECT id, orderId, type, params, price, quantity, printerId, 
         sides, sheets, waste, clicks 
  FROM items 
  WHERE orderId IN (${placeholders})
`, ...orderIds);

// Группируем по orderId
const itemsByOrderId = new Map<number, any[]>();
for (const item of allItems) {
  if (!itemsByOrderId.has(item.orderId)) {
    itemsByOrderId.set(item.orderId, []);
  }
  itemsByOrderId.get(item.orderId)!.push(item);
}

// Присваиваем items к заказам
for (const order of orders) {
  const itemsRaw = itemsByOrderId.get(order.id) || [];
  order.items = itemsRaw.map(ir => {
    // обработка...
  });
}
```

**Ожидаемый эффект:** При 100 заказах: 101 запрос → 2 запроса (улучшение в 50 раз).

---

## 3. 📦 Батчинг JSON.parse операций

### Проблема
В циклах выполняется `JSON.parse` для каждого элемента отдельно, что может быть медленным при больших объемах.

### Решение
Использовать try-catch только при реальной ошибке, кешировать результаты:

```typescript
// Вместо:
for (const item of items) {
  try {
    params = JSON.parse(item.params);
  } catch (error) {
    params = {};
  }
}

// Оптимизированный вариант:
const parseParams = (paramsStr: string) => {
  if (!paramsStr) return {};
  try {
    return JSON.parse(paramsStr);
  } catch {
    return { description: 'Ошибка данных' };
  }
};

const parsedItems = items.map(item => ({
  ...item,
  params: parseParams(item.params)
}));
```

---

## 4. 🗄️ Оптимизация запросов с JOIN

### Проблема
В некоторых местах данные загружаются отдельными запросами вместо JOIN.

### Примеры для оптимизации:
- `orderService.searchOrders` - можно добавить LEFT JOIN для items
- `earningsService.recalculateForDate` - можно объединить запросы продуктов и операций

---

## 5. 💾 Кеширование часто запрашиваемых данных

### Кандидаты для кеширования:
1. **Список операций** - редко меняется, часто запрашивается
2. **Статусы заказов** - статичные данные
3. **Категории материалов** - редко меняются
4. **Настройки принтеров** - редко меняются

### Решение
Использовать простой in-memory кеш с TTL:

```typescript
class DataCache {
  private static cache = new Map<string, { data: any; expires: number }>();
  private static TTL = 10 * 60 * 1000; // 10 минут

  static get<T>(key: string): T | null {
    const cached = this.cache.get(key);
    if (!cached || Date.now() > cached.expires) {
      this.cache.delete(key);
      return null;
    }
    return cached.data as T;
  }

  static set(key: string, data: any, ttl: number = this.TTL) {
    this.cache.set(key, {
      data,
      expires: Date.now() + ttl
    });
  }

  static invalidate(pattern?: string) {
    if (pattern) {
      for (const key of this.cache.keys()) {
        if (key.includes(pattern)) {
          this.cache.delete(key);
        }
      }
    } else {
      this.cache.clear();
    }
  }
}
```

---

## 6. 🔍 Оптимизация поисковых запросов

### Проблема
В `orderService.searchOrders` построение WHERE clause может быть оптимизировано.

### Текущий код:
```typescript
let whereClause = 'WHERE 1=1'
if (filters.userId) {
  whereClause += ' AND userId = ?'
  params.push(filters.userId)
}
// ... и т.д.
```

### Оптимизация:
Использовать подготовленные условия и индексы:
- Убедиться, что есть индексы на `orders.userId`, `orders.status`, `orders.created_at`
- Использовать параметризованные запросы (уже используется ✅)

---

## 7. 📊 Индексы базы данных

### Проверка существующих индексов:
✅ Есть индексы на:
- `orders(status)`, `orders(userId)`, `orders(created_at)`
- `items(orderId)`
- `materials(category_id)`
- `product_operations_link(product_id, operation_id)`

### Рекомендуемые дополнительные индексы:
```sql
-- Для поиска заказов по дате создания
CREATE INDEX IF NOT EXISTS idx_orders_created_at_date 
ON orders(DATE(created_at));

-- Для поиска items по типу
CREATE INDEX IF NOT EXISTS idx_items_type 
ON items(type);

-- Для поиска клиентов по телефону/email (уже есть ✅)
-- Для поиска по customer_id в orders (уже есть ✅)
```

---

## 8. 🧹 Устранение дублирования кода

### Проблема
Множественные места с одинаковой логикой проверки колонок и построения запросов.

### Решение
Вынести в утилиты:
- `tableSchemaCache.ts` - для проверки колонок
- `queryBuilder.ts` - для построения динамических запросов
- `dataMapper.ts` - для маппинга данных из БД

---

## 9. ⚡ Ленивая загрузка данных

### Проблема
В некоторых местах загружаются все данные, даже если нужна только часть.

### Решение
Использовать пагинацию везде, где это возможно:
- Списки заказов ✅ (уже есть)
- Списки материалов - проверить
- Списки операций - проверить

---

## 10. 🔐 Оптимизация валидации

### Проблема
Валидация может выполняться несколько раз для одних и тех же данных.

### Решение
Кешировать результаты валидации или использовать более эффективные проверки.

---

## Приоритеты оптимизации

### Высокий приоритет (быстрый эффект):
1. ✅ Кеширование PRAGMA table_info (легко, большой эффект)
2. ✅ Устранение N+1 в orderService.searchOrders (критично)
3. ✅ Добавление недостающих индексов

### Средний приоритет:
4. Кеширование статических данных (операции, статусы)
5. Оптимизация JSON.parse операций
6. Устранение дублирования кода

### Низкий приоритет (рефакторинг):
7. Вынос утилит
8. Улучшение структуры запросов

---

## Метрики для измерения

После оптимизаций стоит измерить:
- Время ответа API endpoints
- Количество запросов к БД на операцию
- Использование памяти
- Время загрузки страниц (frontend)

---

## Следующие шаги

1. Создать `tableSchemaCache.ts`
2. Оптимизировать `orderService.searchOrders`
3. Добавить недостающие индексы
4. Внедрить кеширование для статических данных
5. Профилировать производительность до/после
