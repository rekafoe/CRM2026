# 📝 Руководство по использованию плейсхолдеров в Word шаблонах

## Основы

В Word документах (`.docx`) для указания изменяемых полей используется синтаксис с **фигурными скобками** `{название_поля}`.

## Простые поля (замена текста)

Просто напишите название поля в фигурных скобках прямо в тексте документа:

```
ДОГОВОР № {contractNumber}

Клиент: {customerName}
Юридическое название: {legalName}
Юридический адрес: {legalAddress}
УНП: {taxId}
Банковские реквизиты: {bankDetails}
Уполномоченное лицо: {authorizedPerson}
Дата договора: {contractDate}
Общая сумма: {totalAmount} BYN
```

**Важно:** 
- Названия полей чувствительны к регистру: `{customerName}` ≠ `{CustomerName}`
- Пробелы в названиях полей не допускаются, используйте camelCase: `{customerName}`, а не `{customer name}`

## Циклы для массивов (таблицы заказов)

Для вывода списка заказов используйте цикл:

### Вариант 1: Простой список

```
ЗАКАЗЫ:

{#orders}
Заказ №{number} от {date} на сумму {amount} BYN (Статус: {status})
{/orders}
```

### Вариант 2: Таблица в Word

Создайте таблицу в Word и используйте цикл:

```
| № | Дата | Номер заказа | Сумма | Статус |
|---|---|---|---|---|
{#orders}
| {number} | {date} | {number} | {amount} | {status} |
{/orders}
```

**Синтаксис цикла:**
- `{#orders}` - начало цикла (обязательно с `#`)
- `{/orders}` - конец цикла (обязательно с `/`)
- Между ними можно использовать любые поля из объекта заказа: `{number}`, `{date}`, `{amount}`, `{status}`

## Условные блоки

Если нужно показывать блок только при определенных условиях:

```
{#hasPrepayment}
Предоплата получена: {prepaymentAmount} BYN
{/hasPrepayment}
```

Или обратное условие (показывать, если значение false/null):

```
{^hasPrepayment}
Предоплата не получена
{/hasPrepayment}
```

## Доступные поля системы

### Данные клиента:
- `{customerName}` - Название клиента
- `{companyName}` - Название компании  
- `{legalName}` - Юридическое название
- `{legalAddress}` - Юридический адрес
- `{taxId}` - УНП
- `{bankDetails}` - Банковские реквизиты
- `{authorizedPerson}` - Уполномоченное лицо

### Данные документа:
- `{contractNumber}` - Номер договора (генерируется автоматически, если не указан)
- `{contractDate}` - Дата договора (текущая дата, если не указана)
- `{totalAmount}` - Общая сумма (автоматически вычисляется из orders, если не указана)

### Массив заказов:
- `{#orders}...{/orders}` - Цикл по заказам
  - `{number}` - Номер заказа
  - `{date}` - Дата заказа
  - `{amount}` - Сумма заказа
  - `{status}` - Статус заказа

## Пример полного шаблона договора

```
ДОГОВОР № {contractNumber}
на оказание полиграфических услуг

Дата: {contractDate}

ИСПОЛНИТЕЛЬ:
ООО "Ваша компания"
УНП: 123456789
Адрес: г. Минск, ул. Примерная, 1

ЗАКАЗЧИК:
{legalName}
УНП: {taxId}
Адрес: {legalAddress}
Расчётный счёт: {bankDetails}
Уполномоченное лицо: {authorizedPerson}

ЗАКАЗЫ:
{#orders}
Заказ №{number} от {date} на сумму {amount} BYN (Статус: {status})
{/orders}

ИТОГО: {totalAmount} BYN
```

## Важные замечания

1. **Не используйте пробелы** в названиях полей: `{customerName}` ✅, `{customer name}` ❌

2. **Регистр важен**: `{customerName}` ≠ `{CustomerName}`

3. **Для циклов обязательны** открывающий `{#fieldName}` и закрывающий `{/fieldName}` теги

4. **Плейсхолдеры можно использовать** в любом месте документа: в тексте, таблицах, заголовках, колонтитулах

5. **Если поле не найдено**, оно будет заменено на пустую строку

6. **Форматирование сохраняется**: стили, шрифты, цвета из шаблона сохраняются в итоговом документе

## Как проверить шаблон

1. Загрузите шаблон через интерфейс "Шаблоны документов"
2. Нажмите "Настроить поля" - система автоматически найдет все плейсхолдеры
3. Сопоставьте найденные поля с полями системы
4. Сохраните маппинг
5. Сгенерируйте тестовый документ

## Частые ошибки

❌ **Неправильно:**
```
{ customerName }  // пробелы внутри скобок
{customer name}    // пробелы в названии
{#orders}          // забыли закрывающий тег
```

✅ **Правильно:**
```
{customerName}
{#orders}...{/orders}
```

## Дополнительные возможности

Если нужно использовать более сложную логику (вычисления, форматирование), можно:
1. Выполнить вычисления на бэкенде перед генерацией
2. Передать уже отформатированные значения в шаблон
3. Использовать несколько полей для разных форматов одного значения
