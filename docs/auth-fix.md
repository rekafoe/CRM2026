# ✅ Исправление ошибки 401 при логине

## Проблема

```
🔴 GET / - 401 - 2ms
```

## Причина

Middleware авторизации (`authMiddleware`) применялся **ко всем маршрутам**, включая корневой путь `/`, который не был добавлен в список исключений (`openPaths`).

## Что исправлено

### 1. Добавлен корневой путь в исключения (`backend/src/middleware/auth.ts`)

```typescript
const openPaths = [
  // Root and static paths
  /^\/$/,                    // ← Корневой путь
  /^\/uploads\//,            // ← Статические файлы
  /^\/api\/uploads\//,       // ← API статических файлов
  // ... остальные пути
]
```

### 2. Добавлен обработчик для корневого пути (`backend/src/index.ts`)

```typescript
// Root endpoint (before auth middleware)
app.get('/', (req, res) => {
  res.json({ 
    message: 'CRM API Server',
    version: '1.0.0',
    status: 'running',
    timestamp: new Date().toISOString()
  })
})
```

Теперь при обращении к `GET /` вы получите:

```json
{
  "message": "CRM API Server",
  "version": "1.0.0",
  "status": "running",
  "timestamp": "2025-02-02T15:30:00.000Z"
}
```

## Проверка

### До исправления:
```bash
curl http://localhost:3000/
# 401 Unauthorized
```

### После исправления:
```bash
curl http://localhost:3000/
# {
#   "message": "CRM API Server",
#   "version": "1.0.0",
#   "status": "running",
#   "timestamp": "..."
# }
```

## Дополнительные исправления

Также добавлены в `openPaths` (если еще не были):
- `/uploads/` - доступ к загруженным файлам
- `/api/uploads/` - API для загруженных файлов

## Порядок middleware в приложении

```
1. CORS
2. Compression
3. Performance monitoring
4. Body parsing (JSON/URL-encoded)
5. Static files (/uploads)
6. Root endpoint (/)           ← без auth
7. Health check (/health)      ← без auth
8. Auth middleware             ← проверяет токен
9. API routes (/api/*)         ← требует auth (кроме openPaths)
10. Error handler
```

## Список публичных путей (без авторизации)

Все эти пути доступны без токена:

```
✅ /                                    # Корневой путь
✅ /health                              # Health check
✅ /uploads/*                           # Статические файлы
✅ /api/auth/login                      # Логин
✅ /api/auth/me                         # Проверка текущего пользователя
✅ /api/products                        # Список продуктов
✅ /api/products/:id                    # Детали продукта
✅ /api/products/:id/calculate          # Расчет цены
✅ /api/orders                          # Создание заказа
✅ /api/orders/:id/prepay               # Предоплата
✅ /api/operations                      # Список операций (NEW!)
✅ ... и другие (см. middleware/auth.ts)
```

## Если проблема осталась

### 1. Проверьте логи:
```bash
cd backend
npm run dev
```

Ищите строки:
```
🔍 Auth middleware: GET /
🔍 Is open path: true
✅ Allowing access to /
```

### 2. Проверьте CORS:
Убедитесь, что фронтенд URL добавлен в `config.corsOrigin`:

```typescript
// backend/src/config/app.ts
cors: {
  origin: process.env.CORS_ORIGIN || 'http://localhost:5173'
}
```

### 3. Проверьте токен на фронтенде:
```javascript
// Убедитесь, что токен передается правильно
const token = localStorage.getItem('api_token');
axios.defaults.headers.common['Authorization'] = `Bearer ${token}`;
```

### 4. Перезапустите сервер:
```bash
cd backend
npm run dev
```

## Статус

✅ Ошибка 401 для корневого пути исправлена  
✅ Добавлены обработчики для `/` и `/health`  
✅ Обновлен список публичных путей  
✅ Компиляция прошла успешно  

---

**Дата:** 2025-02-02  
**Проблема:** 401 Unauthorized на GET /  
**Решение:** Добавлен корневой путь в исключения auth middleware

