# 🚀 Предложения по упрощению настройки продукта

## Текущие проблемы

1. **Слишком много вкладок** (6 вкладок) - сложно ориентироваться
2. **Много кнопок "Сохранить"** - неясно, что нужно сохранять и когда
3. **Нет пошагового процесса** для новых продуктов
4. **Основные поля в модальном окне** - неудобно для частого редактирования
5. **Нет автосохранения** - легко потерять изменения

## Предложения по улучшению

### 1. ✅ Объединить вкладки в логические группы (УЖЕ СДЕЛАНО)

- Добавлены иконки и описания к вкладкам
- Более понятные названия: "📐 Основы", "📦 Материалы", "⚙️ Операции"

### 2. Автосохранение изменений

**Проблема:** Пользователь должен помнить нажимать "Сохранить" в каждой секции

**Решение:**
- Автоматически сохранять изменения через 2-3 секунды после редактирования
- Показывать индикатор "Сохранено" / "Сохранение..."
- Оставить кнопку "Сохранить шаблон" для принудительного сохранения всех изменений

**Реализация:**
```typescript
// Использовать debounce для автосохранения
const debouncedSave = useMemo(
  () => debounce(() => persistTemplateConfig('Автосохранено'), 2000),
  [persistTemplateConfig]
);

// Вызывать при изменении state
useEffect(() => {
  if (!loading) {
    debouncedSave();
  }
}, [state, debouncedSave, loading]);
```

### 3. Мастер настройки для новых продуктов

**Проблема:** Новый продукт требует настройки множества параметров без понятного порядка

**Решение:**
- Показывать пошаговый мастер при первом открытии нового продукта
- Шаги:
  1. Основные поля (название, описание, иконка)
  2. Формат и размеры
  3. Материалы (минимум 1 обязательный)
  4. Операции (минимум 1 обязательная)
  5. Параметры калькулятора (опционально)
  6. Ценообразование (опционально)

**Реализация:**
```typescript
const [showSetupWizard, setShowSetupWizard] = useState(false);

useEffect(() => {
  // Показывать мастер, если продукт новый (нет материалов и операций)
  if (!loading && product && materials.length === 0 && operationsLength === 0) {
    setShowSetupWizard(true);
  }
}, [loading, product, materials.length, operationsLength]);
```

### 4. Интеграция с ProductSetupStatus

**Проблема:** Статус настройки есть, но он не связан с интерфейсом

**Решение:**
- Показывать прогресс настройки в топ-баре
- Подсвечивать незаполненные вкладки
- Предлагать перейти к следующему шагу

**Реализация:**
```typescript
// В топ-баре показывать прогресс
<div className="setup-progress-indicator">
  <ProgressBar 
    value={completedSteps / totalSteps} 
    label={`${completedSteps}/${totalSteps} шагов`}
  />
</div>

// Подсвечивать вкладки с незаполненными обязательными полями
{tabs.map(tab => (
  <button
    className={`
      template-tab 
      ${activeTab === tab.key ? 'template-tab--active' : ''}
      ${!isTabComplete(tab.key) ? 'template-tab--incomplete' : ''}
    `}
  >
    {tab.label}
    {!isTabComplete(tab.key) && <span className="required-badge">!</span>}
  </button>
))}
```

### 5. Упростить основные поля продукта

**Проблема:** Основные поля в модальном окне - неудобно для частого редактирования

**Решение:**
- Вернуть основные поля в основную вкладку "Основы"
- Сделать их свернутыми по умолчанию с кнопкой "Развернуть"
- Или показывать компактную версию в топ-баре с возможностью редактирования

### 6. Группировка настроек

**Проблема:** Много разрозненных секций

**Решение:**
- Объединить "Отделка" и "Упаковка" в одну вкладку
- Объединить "Ценообразование" с "Параметрами" (они связаны)

### 7. Подсказки и валидация

**Проблема:** Неясно, что обязательно заполнить

**Решение:**
- Показывать подсказки для обязательных полей
- Валидация в реальном времени
- Показывать предупреждения, если что-то не заполнено

### 8. Быстрые действия

**Проблема:** Частые действия требуют много кликов

**Решение:**
- Кнопка "Дублировать продукт" в топ-баре
- Кнопка "Сбросить к шаблону" для восстановления значений по умолчанию
- Кнопка "Экспорт/Импорт конфигурации"

## Приоритеты

1. **Высокий приоритет:**
   - Автосохранение (уменьшит потерю данных)
   - Мастер настройки для новых продуктов (упростит создание)
   - Интеграция с ProductSetupStatus (покажет прогресс)

2. **Средний приоритет:**
   - Упростить основные поля (улучшит UX)
   - Группировка настроек (упростит навигацию)

3. **Низкий приоритет:**
   - Подсказки и валидация (улучшит понимание)
   - Быстрые действия (удобство)

## Следующие шаги

1. Реализовать автосохранение
2. Добавить мастер настройки
3. Интегрировать статус настройки в интерфейс
4. Упростить основные поля

