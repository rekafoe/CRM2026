# Динамическая схема калькулятора на основе параметров продукта

## 📋 Обзор

Калькулятор теперь работает как **динамический рендерер форм** на основе JSON-схемы из справочника продуктов. Когда вы создаете продукт в админке и настраиваете его параметры, эти параметры автоматически превращаются в поля калькулятора.

## 🎯 Как это работает

### 1. Создание продукта в админке

1. Перейдите в `http://localhost:5173/adminpanel/products`
2. Создайте новый продукт или отредактируйте существующий
3. Перейдите на вкладку **"Параметры"**
4. Добавьте параметры продукта:
   - **name**: имя поля (например, `color`, `size`, `orientation`)
   - **label**: подпись поля для пользователя (например, "Цвет", "Размер", "Ориентация")
   - **type**: тип поля (`text`, `number`, `select`, `checkbox`)
   - **options**: варианты для select (например, `red;blue;green`)
   - **is_required**: обязательное поле или нет
   - **min_value / max_value**: для числовых полей

### 2. Активация продукта

- Переключите статус продукта в **"Активен"**
- Продукт появится в списке доступных продуктов для калькулятора

### 3. Использование в калькуляторе

1. Откройте калькулятор
2. Нажмите кнопку **"Выбор продукта"**
3. Выберите ваш продукт из списка
4. Калькулятор автоматически отрисует все поля на основе параметров продукта

## 🔧 Техническая реализация

### Backend API

**Эндпоинт:** `GET /api/products/:productId/schema`

**Ответ:**
```json
{
  "data": {
    "id": 123,
    "key": "custom_product",
    "name": "Кастомный продукт",
    "type": "Кастомный продукт",
    "description": "Описание продукта",
    "fields": [
      {
        "name": "material_id",
        "label": "Материал",
        "type": "string",
        "required": true,
        "enum": [
          {
            "value": 45,
            "label": "Мелованная бумага 300 г/м² (0.50 Br)",
            "price": 0.5
          }
        ]
      },
      {
        "name": "color",
        "label": "Цвет",
        "type": "string",
        "required": true,
        "enum": ["red", "blue", "green"]
      },
      {
        "name": "quantity",
        "label": "Количество",
        "type": "number",
        "required": true,
        "min": 1,
        "max": 10000
      }
    ],
    "materials": [
      {
        "id": 45,
        "name": "Мелованная бумага 300 г/м²",
        "price": 0.5,
        "unit": "лист",
        "is_required": true
      }
    ]
  }
}
```

### Frontend

**Хук:** `useCalculatorSchema`

```typescript
const { backendProductSchema, currentConfig, availableFormats, getDefaultFormat } = useCalculatorSchema({
  productType: specs.productType,
  productId: selectedProduct?.id || null, // 🆕 Передаем ID выбранного продукта
  log: logger,
  setSpecs
});
```

**Логика:**
1. Если передан `productId`, загружается схема по ID продукта через `/api/products/:productId/schema`
2. Если `productId` не передан, загружается старая схема по типу продукта через `/api/pricing/product-types/:key/schema`
3. Схема кэшируется для повышения производительности
4. При смене продукта схема перезагружается

### Отрисовка полей

**Компонент:** `DynamicFieldsSection`

- Автоматически рендерит поля на основе `schema.fields`
- Поддерживает типы: `string`, `number`, `boolean`, `select`
- Обрабатывает enum-поля с вариантами
- Применяет валидацию (required, min, max)

## 📦 Пример: Создание продукта "Кружка с принтом"

### Шаг 1: Создаем продукт

```sql
INSERT INTO products (name, description, category_id, is_active) 
VALUES ('Кружка с принтом', 'Белая керамическая кружка с цветным принтом', 1, 1);
```

### Шаг 2: Добавляем параметры

```sql
-- Материал (автоматически добавляется при добавлении материалов в product_materials)

-- Цвет ручки
INSERT INTO product_parameters (product_id, name, label, type, options, is_required, sort_order) 
VALUES (
  (SELECT id FROM products WHERE name = 'Кружка с принтом'),
  'handle_color',
  'Цвет ручки',
  'select',
  '["Белый", "Чёрный", "Красный"]',
  0,
  1
);

-- Размер принта
INSERT INTO product_parameters (product_id, name, label, type, options, is_required, sort_order) 
VALUES (
  (SELECT id FROM products WHERE name = 'Кружка с принтом'),
  'print_size',
  'Размер принта',
  'select',
  '["Маленький", "Средний", "Большой"]',
  1,
  2
);

-- Количество
INSERT INTO product_parameters (product_id, name, label, type, is_required, min_value, max_value, sort_order) 
VALUES (
  (SELECT id FROM products WHERE name = 'Кружка с принтом'),
  'quantity',
  'Количество',
  'number',
  1,
  1,
  1000,
  3
);
```

### Шаг 3: Добавляем материалы

```sql
-- Связываем продукт с материалом (керамическая кружка)
INSERT INTO product_materials (product_id, material_id, is_required) 
VALUES (
  (SELECT id FROM products WHERE name = 'Кружка с принтом'),
  (SELECT id FROM materials WHERE name LIKE '%Кружка%' LIMIT 1),
  1
);
```

### Шаг 4: Результат

Когда пользователь выберет "Кружка с принтом" в калькуляторе, он увидит:
- **Материал**: выпадающий список с керамическими кружками
- **Цвет ручки**: Белый / Чёрный / Красный
- **Размер принта**: Маленький / Средний / Большой (обязательное)
- **Количество**: числовое поле от 1 до 1000 (обязательное)

## 🎨 Преимущества

1. **Гибкость**: Создавайте любые продукты без изменения кода
2. **Простота**: Настройка через админ-панель, а не через код
3. **Масштабируемость**: Добавляйте новые параметры без остановки системы
4. **Валидация**: Автоматическая валидация на основе типов и ограничений
5. **Переиспользование**: Один калькулятор для всех типов продуктов

## 🔄 Обратная совместимость

Старые продукты (flyers, business_cards, booklets) продолжают работать через старую систему типов продуктов. Новые продукты используют динамическую схему.

## 📚 Связанные файлы

### Backend
- `backend/src/modules/products/routes/products.ts` - роут `/products/:productId/schema`
- `backend/src/modules/pricing/controllers/pricingController.ts` - роут `/pricing/product-types/:key/schema`

### Frontend
- `frontend/src/components/calculator/hooks/useCalculatorSchema.ts` - загрузка схем
- `frontend/src/components/calculator/components/DynamicFieldsSection.tsx` - отрисовка полей
- `frontend/src/components/calculator/components/DynamicProductSelector.tsx` - выбор продукта
- `frontend/src/components/calculator/ImprovedPrintingCalculatorModal.tsx` - главный калькулятор

## 🐛 Отладка

Включите логи в консоли браузера:
- `📦 Загружаем схему по ID продукта` - загрузка схемы по ID
- `📋 Загружаем схему по типу продукта` - загрузка старой схемы
- `✅ Схема продукта загружена` - схема успешно загружена с количеством полей

## ✅ Чеклист настройки нового продукта

- [ ] Создан продукт в таблице `products`
- [ ] Установлен статус `is_active = 1`
- [ ] Добавлены материалы в `product_materials`
- [ ] Добавлены параметры в `product_parameters`
- [ ] Добавлены операции в `product_operations_link`
- [ ] Продукт появляется в селекторе калькулятора
- [ ] Поля корректно отрисовываются в калькуляторе
- [ ] Расчет цены работает корректно

