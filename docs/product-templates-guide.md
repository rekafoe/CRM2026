# 📦 Руководство по шаблонам продуктов

## 🎯 Обзор

Система поддерживает создание **динамических продуктов** с настраиваемыми параметрами. Каждый продукт автоматически получает свой калькулятор на основе JSON-схемы из базы данных.

## 🚀 Быстрый старт

### Создание продуктов из шаблонов:

```bash
# Вариант 1: SQL-скрипт
sqlite3 backend/database.db < backend/scripts/seed-product-templates.sql

# Вариант 2: Node.js скрипт
node backend/scripts/seed-product-templates.js
```

Это создаст 4 базовых продукта:
- 📄 **Листовки** (простой)
- 💳 **Визитки** (с вариантами)
- 📚 **Печать документов** (многостраничный)
- 📖 **Брошюры** (комплексный)

---

## 📋 Структура продукта

### 1. Основная информация (таблица `products`)

```sql
INSERT INTO products (
  name,                    -- Название
  description,             -- Описание
  category_id,             -- ID категории
  icon,                    -- Эмодзи иконка
  calculator_type,         -- Тип калькулятора: 'product' | 'operation'
  product_type,            -- Тип продукта
  is_active                -- Активен/неактивен
) VALUES (
  'Листовки',
  'Цветные листовки различных форматов',
  1,
  '📄',
  'product',
  'sheet_single',
  1
);
```

### 2. Параметры (таблица `product_parameters`)

```sql
INSERT INTO product_parameters (
  product_id,              -- ID продукта
  name,                    -- Имя параметра (для API)
  label,                   -- Подпись (для UI)
  type,                    -- Тип: select | number | checkbox | text
  options,                 -- JSON массив опций для select
  min_value,               -- Минимум для number
  max_value,               -- Максимум для number
  is_required,             -- Обязательный/опциональный
  sort_order               -- Порядок отображения
) VALUES (
  1,
  'format',
  'Формат',
  'select',
  '["A6", "A5", "A4", "A3"]',
  NULL,
  NULL,
  1,
  1
);
```

---

## 🎨 Типы полей

### Select (Выпадающий список)

```sql
INSERT INTO product_parameters (product_id, name, label, type, options, is_required, sort_order)
VALUES (1, 'format', 'Формат', 'select', '["A6", "A5", "A4"]', 1, 1);
```

**Отображение:**
```
Формат: [A6 ▼]
```

### Number (Числовое поле)

```sql
INSERT INTO product_parameters (product_id, name, label, type, min_value, max_value, is_required, sort_order)
VALUES (1, 'quantity', 'Количество', 'number', 1, 10000, 1, 2);
```

**Отображение:**
```
Количество: [-] [100] [+]
```

### Checkbox (Чекбокс)

```sql
INSERT INTO product_parameters (product_id, name, label, type, is_required, sort_order)
VALUES (1, 'lamination', 'Ламинирование', 'checkbox', 0, 3);
```

**Отображение:**
```
☐ Ламинирование
```

### Text (Текстовое поле)

```sql
INSERT INTO product_parameters (product_id, name, label, type, is_required, sort_order)
VALUES (1, 'notes', 'Примечания', 'text', 0, 4);
```

---

## 📚 Примеры продуктов

### 1. Простой продукт (Листовки)

**Характеристики:**
- Одностраничный
- Фиксированный формат
- Простые опции

**Параметры:**
- Формат (select)
- Количество (number)
- Стороны печати (select)
- Материал (select)
- Ламинирование (checkbox)

### 2. Многостраничный продукт (Документы)

**Характеристики:**
- Количество страниц
- Тип переплета
- Разные типы печати

**Параметры:**
- **Страницы** (number) - от 1 до 1000
- Количество экземпляров (number)
- Формат (select)
- Тип печати (select)
- **Тип переплета** (select) - ключевой параметр!
- Двухсторонняя печать (checkbox)

### 3. Комплексный продукт (Брошюры)

**Характеристики:**
- Обложка отдельно
- Блок отдельно
- Разные материалы для обложки и блока

**Параметры:**
- Страницы (number)
- Тираж (number)
- **Обложка:** тип, материал, ламинация
- **Блок:** тип печати, материал
- Переплет (select)

---

## 🔧 Связывание с материалами

После создания продукта нужно связать его с материалами:

```sql
-- Связываем продукт с материалами
INSERT INTO product_materials (product_id, material_id, is_required)
VALUES 
  (1, 5, 1),  -- Мелованная бумага (обязательна)
  (1, 6, 0),  -- Дизайнерская бумага (опциональна)
  (1, 7, 0);  -- Офсетная бумага (опциональна)
```

**В калькуляторе автоматически появится:**
```
Материал: [Мелованная бумага ▼]
          [Дизайнерская бумага]
          [Офсетная бумага]
```

---

## ⚙️ Связывание с операциями

```sql
-- Связываем продукт с операциями
INSERT INTO product_operations_link (product_id, operation_id, sequence, is_required)
VALUES 
  (1, 1, 1, 1),  -- Печать (обязательна)
  (1, 2, 2, 1),  -- Резка (обязательна)
  (1, 3, 3, 0);  -- Ламинирование (опциональна)
```

---

## 🎯 Типы продуктов (product_type)

### `sheet_single` - Листовые изделия
- Листовки
- Плакаты
- Афиши
- **Расчет:** за лист

### `sheet_item` - Листовые штучные
- Визитки
- Наклейки
- Карточки
- **Расчет:** за штуку (несколько на листе)

### `multi_page` - Многостраничные
- Документы
- Методички
- Отчеты
- **Расчет:** страницы × экземпляры

### `multi_page_item` - Многостраничные изделия
- Брошюры
- Каталоги
- Журналы
- **Расчет:** сложная формула с обложкой

### `universal` - Универсальный
- Любые нестандартные продукты
- **Расчет:** индивидуальный

---

## 💡 Best Practices

### ✅ DO (Делайте так)

1. **Используйте понятные имена параметров**
   ```sql
   name = 'paper_density'  -- ✅ Хорошо
   name = 'pd'             -- ❌ Плохо
   ```

2. **Добавляйте описательные labels**
   ```sql
   label = 'Плотность бумаги (г/м²)'  -- ✅ Понятно
   label = 'Плотность'                -- ⚠️ Неплохо
   ```

3. **Сортируйте параметры логично**
   ```
   1. Основные (формат, количество)
   2. Материалы (бумага, плотность)
   3. Дополнительные услуги (ламинирование)
   4. Срочность
   ```

4. **Группируйте связанные параметры**
   ```sql
   -- Группа "Обложка"
   sort_order = 10  -- cover_type
   sort_order = 11  -- cover_material
   sort_order = 12  -- cover_lamination
   
   -- Группа "Блок"
   sort_order = 20  -- block_print_type
   sort_order = 21  -- block_material
   ```

### ❌ DON'T (Не делайте так)

1. **Не дублируйте параметры**
   ```sql
   -- ❌ Плохо
   name = 'quantity'
   name = 'qty'
   name = 'amount'
   ```

2. **Не создавайте слишком много опций**
   ```sql
   -- ❌ 20+ вариантов в select
   options = '["A6", "A5", "A4", "A3", "B5", "B4", ...]'
   
   -- ✅ Лучше разбить на категории
   ```

3. **Не забывайте `is_required`**
   ```sql
   -- ❌ Все обязательные
   is_required = 1
   is_required = 1
   is_required = 1
   
   -- ✅ Опциональные допуслуги
   is_required = 1  -- format
   is_required = 0  -- lamination
   ```

---

## 🧪 Тестирование

После создания продукта:

1. **Проверьте в админке:**
   ```
   http://localhost:5173/adminpanel/products
   ```

2. **Откройте калькулятор:**
   - Нажмите "Выбор продукта"
   - Найдите свой продукт
   - Проверьте, что все поля отображаются

3. **Попробуйте рассчитать:**
   - Заполните все обязательные поля
   - Нажмите "Рассчитать"
   - Проверьте результат

---

## 🔄 Обновление продуктов

### Добавить новый параметр:

```sql
INSERT INTO product_parameters (product_id, name, label, type, options, is_required, sort_order)
VALUES (1, 'corner_rounding', 'Скругление углов', 'checkbox', NULL, 0, 99);
```

### Изменить параметр:

```sql
UPDATE product_parameters 
SET options = '["A6", "A5", "A4", "A3", "A2"]'
WHERE product_id = 1 AND name = 'format';
```

### Удалить параметр:

```sql
DELETE FROM product_parameters 
WHERE product_id = 1 AND name = 'corner_rounding';
```

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи калькулятора в консоли браузера
2. Убедитесь, что продукт активен (`is_active = 1`)
3. Проверьте, что есть хотя бы один материал и операция
4. Проверьте JSON-валидность в поле `options`

---

## ✅ Чеклист создания продукта

- [ ] Создан продукт в `products`
- [ ] Добавлены параметры в `product_parameters`
- [ ] Связаны материалы в `product_materials`
- [ ] Добавлены операции в `product_operations_link`
- [ ] Продукт активирован (`is_active = 1`)
- [ ] Протестирован в калькуляторе
- [ ] Проверен расчет цены
- [ ] Проверено добавление в заказ

🎉 **Готово!** Теперь продукт доступен для заказа!

