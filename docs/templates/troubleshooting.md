# 🔧 Устранение неполадок с шаблонами документов

## Проблема: Документ не генерируется на основе загруженного шаблона

### Возможные причины и решения:

#### 1. Шаблон не установлен как шаблон по умолчанию

**Проблема:** Система ищет шаблон с флагом `is_default = 1`. Если вы загрузили шаблон, но не установили его как шаблон по умолчанию, система может не найти его.

**Решение:**
1. Откройте страницу "Шаблоны документов" в админ-панели
2. Найдите ваш загруженный шаблон
3. Нажмите кнопку "По умолчанию" рядом с шаблоном

**Примечание:** Система автоматически использует первый доступный шаблон, если шаблон по умолчанию не найден (начиная с версии с автоматическим маппингом).

#### 2. Ошибка в структуре шаблона

**Проблема:** Если в шаблоне есть синтаксические ошибки в плейсхолдерах, генерация может не работать.

**Решение:**
- Проверьте, что все плейсхолдеры написаны правильно: `${fieldName}`
- Для таблиц используйте правильный синтаксис: `${table:orderItems}`
- Убедитесь, что после `${table:orderItems}` есть строка с плейсхолдерами полей

#### 3. Проблема с путём к файлу шаблона

**Проблема:** Файл шаблона может быть удалён или перемещён после загрузки.

**Решение:**
1. Проверьте логи сервера на наличие ошибок типа "Файл шаблона не найден"
2. Перезагрузите шаблон в систему

#### 4. Проблема с автоматическим маппингом

**Проблема:** Если в шаблоне используются нестандартные имена полей, автоматический маппинг может не сработать.

**Решение:**
1. Используйте стандартные имена полей (см. список ниже)
2. Или настройте маппинг вручную через кнопку "Настроить поля (опционально)"

### Стандартные имена полей для автоматического маппинга:

- `${customerName}` - Название клиента
- `${legalName}` - Юридическое наименование
- `${companyName}` - Название компании
- `${taxId}` - УНП
- `${bankDetails}` - Банковские реквизиты
- `${authorizedPerson}` - Уполномоченное лицо
- `${legalAddress}` - Адрес
- `${contractNumber}` - Номер договора/акта/счета
- `${contractDate}` - Дата
- `${totalAmount}` - Общая сумма
- `${totalQuantity}` - Общее количество
- `${totalAmountInWords}` - Сумма прописью
- `${table:orderItems}` - Таблица позиций
- `${table:orders}` - Таблица заказов

### Проверка работы шаблона:

1. **Проверьте консоль браузера** (F12) на наличие ошибок
2. **Проверьте логи сервера** на наличие сообщений об ошибках
3. **Проверьте, что шаблон загружен:**
   - Откройте страницу "Шаблоны документов"
   - Убедитесь, что ваш шаблон отображается в списке
   - Проверьте, что тип шаблона указан правильно (Акт, Договор, Счёт)

### Отладка:

Если проблема не решена, проверьте:

1. **В консоли браузера:**
   - Откройте DevTools (F12)
   - Перейдите на вкладку "Console"
   - Попробуйте сгенерировать документ
   - Посмотрите на ошибки (красные сообщения)

2. **В логах сервера:**
   - Запустите сервер в режиме разработки
   - Посмотрите на сообщения с префиксом `[DocumentTemplate]`
   - Они покажут, какой шаблон используется и какие данные передаются

3. **Проверьте данные:**
   - Убедитесь, что у выбранного клиента есть заказы
   - Проверьте, что заказы содержат позиции (items)

### Частые ошибки:

1. **"Шаблон для типа 'act' не найден"**
   - Решение: Загрузите шаблон типа "Акт" или установите существующий как шаблон по умолчанию

2. **"Ошибка генерации документа: ..."**
   - Решение: Проверьте структуру шаблона и плейсхолдеры

3. **Документ генерируется, но пустой**
   - Решение: Проверьте, что данные передаются правильно и что плейсхолдеры в шаблоне соответствуют данным

### Получение помощи:

Если проблема не решена:
1. Сохраните скриншот ошибки из консоли браузера
2. Сохраните логи сервера
3. Проверьте структуру вашего шаблона
4. Обратитесь к разработчику с этой информацией
