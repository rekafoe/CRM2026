# 📊 Руководство по использованию плейсхолдеров в Excel шаблонах

## Основы

В Excel файлах (`.xlsx` или `.xls`) для указания изменяемых полей используется синтаксис с **долларом и фигурными скобками** `${название_поля}`.

## Простые поля (замена текста)

В любой ячейке Excel напишите плейсхолдер:

```
| A | B |
|---|---|
| Клиент | ${customerName} |
| УНП | ${taxId} |
| Общая сумма | ${totalAmount} |
```

## Таблицы позиций (для актов и счетов)

Для таблицы с позициями заказов используйте специальный синтаксис `${table:orderItems}`:

### Структура таблицы в Excel

Создайте таблицу в Excel следующим образом:

```
| № | Наименование работы (услуги) | Единица изме- | Количество | Цена, руб.коп. | Сумма, руб.коп. | Ставка НДС, % | Сумма НДС, руб.коп. | Всего с НДС, руб.коп. |
|---|---|---|---|---|---|---|---|---|
| ${table:orderItems} | | | | | | | | |
| ${number} | ${name} | ${unit} | ${quantity} | ${price} | ${amount} | ${vatRate} | ${vatAmount} | ${totalWithVat} |
```

**Важно:**
- Первая строка после `${table:orderItems}` - это заголовки таблицы
- Вторая строка - это шаблон строки данных
- Библиотека xlsx-template автоматически создаст столько строк, сколько элементов в массиве `orderItems`

### Доступные поля для позиций

Каждая позиция в массиве `orderItems` содержит:
- `number` - Порядковый номер (1, 2, 3...)
- `name` - Наименование работы/услуги
- `unit` - Единица измерения (шт, м², лист и т.д.)
- `quantity` - Количество
- `price` - Цена за единицу
- `amount` - Сумма (quantity × price)
- `vatRate` - Ставка НДС (например, "Без НДС" или "20%")
- `vatAmount` - Сумма НДС
- `totalWithVat` - Всего с НДС

## Пример полного шаблона акта

```
| АКТ выполненных работ № 1 |
| |
| Исполнитель: ООО "Светлан Эстетикс" |
| Заказчик: ${legalName} |
| УНП: ${taxId} |
| |
| № | Наименование работы (услуги) | Единица изме- | Количество | Цена, руб.коп. | Сумма, руб.коп. | Ставка НДС, % | Сумма НДС, руб.коп. | Всего с НДС, руб.коп. |
| ${table:orderItems} | | | | | | | | |
| ${number} | ${name} | ${unit} | ${quantity} | ${price} | ${amount} | ${vatRate} | ${vatAmount} | ${totalWithVat} |
| | | | | Итого: | ${totalAmount} | | | |
```

## Как работает таблица при большом количестве позиций

1. **Автоматическое расширение**: Библиотека xlsx-template автоматически создает столько строк, сколько элементов в массиве `orderItems`

2. **Перенос на новую страницу**: Excel автоматически переносит таблицу на следующую страницу, если она не помещается на одной странице

3. **Итоговые строки**: Итоговые строки (Итого, Всего с НДС) должны быть размещены после таблицы, вне блока `${table:orderItems}`

## Рекомендации по созданию шаблона

1. **Создайте заголовок таблицы** в первой строке после `${table:orderItems}`
2. **Создайте шаблон строки** во второй строке с плейсхолдерами полей
3. **Разместите итоговые строки** после таблицы (вне блока `${table:orderItems}`)
4. **Форматирование**: Примените форматирование к шаблонной строке - оно будет применено ко всем созданным строкам

## Пример с итоговыми строками

```
| ${table:orderItems} | | | | | | | | |
| ${number} | ${name} | ${unit} | ${quantity} | ${price} | ${amount} | ${vatRate} | ${vatAmount} | ${totalWithVat} |
| | | | | | | | | |
| | | | | Итого: | ${totalAmount} | | | |
| | | | | Всего с НДС: | ${totalAmount} | | | |
```

## Важные замечания

1. **Порядок важен**: `${table:orderItems}` должен быть в первой ячейке строки с заголовками
2. **Шаблон строки обязателен**: После `${table:orderItems}` должна быть строка с плейсхолдерами полей
3. **Автоматическое создание строк**: Не нужно создавать пустые строки - система создаст их автоматически
4. **Форматирование сохраняется**: Форматирование из шаблонной строки применяется ко всем созданным строкам

## Ограничения

- Максимальное количество строк в Excel: 1,048,576 (этого более чем достаточно для любых актов)
- Если позиций очень много (тысячи), Excel может работать медленнее, но это нормально

## Отладка

Если таблица не создается:
1. Проверьте, что `${table:orderItems}` написан правильно (без пробелов)
2. Убедитесь, что после `${table:orderItems}` есть строка с плейсхолдерами
3. Проверьте, что в данных есть массив `orderItems`
4. Убедитесь, что каждый элемент массива содержит все необходимые поля
