# 📄 Использование шаблона акта выполненных работ

## Создание шаблона

Шаблон акта был автоматически создан скриптом и находится в файле:
```
d:\CRM\templates\act-template.xlsx
```

Для пересоздания шаблона выполните:
```bash
cd backend
npm run generate:act-template
```

## Структура шаблона

Шаблон содержит следующие разделы:

1. **Информация об исполнителе** (статическая, можно редактировать вручную)
2. **Заголовок документа** с плейсхолдерами `${contractNumber}` и `${contractDate}`
3. **Информация о заказчике** с плейсхолдерами:
   - `${legalName}` - Наименование организации
   - `${bankDetails}` - Банковские реквизиты
   - `${taxId}` - УНП
   - `${legalAddress}` - Адрес
4. **Таблица позиций** с динамическим заполнением через `${table:orderItems}`
5. **Итоговые суммы** с плейсхолдерами `${totalAmount}`
6. **Текстовое резюме** с плейсхолдерами `${totalQuantity}` и `${totalAmountInWords}`
7. **Блоки подписей** с плейсхолдерами `${authorizedPerson}` и `${executorAuthorizedPerson}`

## Доступные плейсхолдеры

### Основные поля
- `${contractNumber}` - Номер акта (автоматически генерируется, если не указан)
- `${contractDate}` - Дата акта (текущая дата, если не указана)
- `${legalName}` - Наименование заказчика
- `${bankDetails}` - Банковские реквизиты заказчика
- `${taxId}` - УНП заказчика
- `${legalAddress}` - Адрес заказчика
- `${totalAmount}` - Общая сумма (автоматически рассчитывается из позиций)
- `${totalQuantity}` - Общее количество позиций
- `${totalAmountInWords}` - Сумма прописью (автоматически генерируется)
- `${authorizedPerson}` - Уполномоченное лицо заказчика
- `${executorAuthorizedPerson}` - Уполномоченное лицо исполнителя (можно настроить в системе)

### Таблица позиций (${table:orderItems})

Для таблицы позиций используется специальный синтаксис xlsx-template:

1. **Маркер начала таблицы**: В первой ячейке строки после заголовков укажите `${table:orderItems}`
2. **Шаблон строки**: В следующей строке укажите плейсхолдеры полей:
   - `${number}` - Порядковый номер (1, 2, 3...)
   - `${name}` - Наименование работы/услуги
   - `${unit}` - Единица измерения (шт, м², лист и т.д.)
   - `${quantity}` - Количество
   - `${price}` - Цена за единицу
   - `${amount}` - Сумма (quantity × price)
   - `${vatRate}` - Ставка НДС (например, "Без НДС" или "20%")
   - `${vatAmount}` - Сумма НДС
   - `${totalWithVat}` - Всего с НДС

**Пример структуры таблицы в Excel:**

```
| № | Наименование работы (услуги) | Единица изме- | Количество | Цена, руб.коп. | Сумма, руб.коп. | Ставка НДС, % | Сумма НДС, руб.коп. | Всего с НДС, руб.коп. |
| ${table:orderItems} | | | | | | | | |
| ${number} | ${name} | ${unit} | ${quantity} | ${price} | ${amount} | ${vatRate} | ${vatAmount} | ${totalWithVat} |
```

## Как работает таблица при большом количестве позиций

1. **Автоматическое расширение**: Библиотека xlsx-template автоматически создает столько строк, сколько элементов в массиве `orderItems`

2. **Перенос на новую страницу**: Excel автоматически переносит таблицу на следующую страницу, если она не помещается на одной странице

3. **Форматирование**: Форматирование из шаблонной строки применяется ко всем созданным строкам

4. **Итоговые строки**: Итоговые строки (Итого, Всего с НДС) должны быть размещены **после** таблицы, вне блока `${table:orderItems}`

## Редактирование шаблона

Вы можете открыть файл `templates/act-template.xlsx` в Excel и:

1. **Изменить форматирование** (шрифты, цвета, выравнивание)
2. **Добавить логотип** компании
3. **Изменить расположение** полей
4. **Добавить дополнительные поля** (если они поддерживаются системой)

**Важно**: При редактировании не удаляйте плейсхолдеры `${...}` и не изменяйте структуру таблицы `${table:orderItems}`.

## Загрузка шаблона в систему

После создания или редактирования шаблона:

1. Откройте страницу "Шаблоны документов" в админ-панели
2. Нажмите "Загрузить шаблон"
3. Выберите файл `act-template.xlsx`
4. Укажите тип: "Акт"
5. Нажмите "Сохранить"

## Использование

После загрузки шаблона:

1. Откройте страницу "Клиенты" в админ-панели
2. Выберите клиента
3. Выберите заказы для акта
4. Нажмите "Экспорт акта"
5. Система автоматически заполнит шаблон данными и сгенерирует файл

## Автоматические расчеты

Система автоматически:
- Рассчитывает `totalAmount` из суммы всех позиций
- Подсчитывает `totalQuantity` (общее количество позиций)
- Генерирует `totalAmountInWords` (сумма прописью)
- Генерирует `contractNumber` в формате `YYYYMM-XXXX`, если не указан
- Использует текущую дату для `contractDate`, если не указана

## Настройка данных исполнителя

Данные об исполнителе (название компании, реквизиты) в шаблоне статические. Для их изменения:

1. Откройте шаблон в Excel
2. Отредактируйте соответствующие ячейки
3. Сохраните и загрузите обновленный шаблон в систему

В будущем можно добавить настройку данных исполнителя в конфигурацию системы.
