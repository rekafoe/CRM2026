# 🔧 Исправление ошибки "Ошибка в части содержимого в книге"

## Проблема

При открытии сгенерированного Excel файла появляется ошибка:
```
Ошибка в части содержимого в книге [имя_файла].xlsx. 
Выполнить попытку восстановления?
```

## Причины

Эта ошибка возникает, когда:
1. **Шаблон был создан программно** (через библиотеку `xlsx`), а не вручную в Excel
2. **Шаблон содержит элементы**, которые `xlsx-template` не может правильно обработать (изображения, графики, макросы)
3. **Структура шаблона несовместима** с `xlsx-template`
4. **Проблемы при генерации** - файл создается, но структура нарушена

## Решения

### Решение 1: Создать шаблон вручную в Excel (РЕКОМЕНДУЕТСЯ)

Это самое надежное решение:

1. **Откройте Excel**
2. **Создайте новый файл**
3. **Создайте структуру шаблона:**
   - Добавьте все необходимые заголовки
   - Вставьте плейсхолдеры в ячейки (например, `${legalName}`, `${totalAmount}`)
   - Для таблиц используйте правильный синтаксис:
     ```
     | Заголовок 1 | Заголовок 2 | ... |
     | ${table:orderItems} | | |
     | ${number} | ${name} | ... |
     ```
4. **Сохраните как `.xlsx`** (File → Save As → Excel Workbook)
5. **Загрузите в систему**

**Важно:**
- НЕ добавляйте изображения
- НЕ добавляйте графики или диаграммы
- НЕ добавляйте макросы
- НЕ используйте сложные формулы
- Используйте простую структуру

### Решение 2: Пересохранить существующий шаблон

Если у вас уже есть шаблон:

1. **Откройте шаблон в Excel**
2. **Нажмите "Да"** на запрос о восстановлении (если появится)
3. **Сохраните файл заново:**
   - File → Save As
   - Выберите формат "Excel Workbook (*.xlsx)"
   - Сохраните
4. **Загрузите обновленный файл в систему**

### Решение 3: Использовать шаблон, созданный скриптом

Если вы используете шаблон из `templates/act-template.xlsx`:

1. **Откройте файл в Excel**
2. **Если появится ошибка восстановления - нажмите "Да"**
3. **Сохраните файл заново** (File → Save As → Excel Workbook)
4. **Это пересоздаст структуру файла** в правильном формате
5. **Загрузите обновленный файл в систему**

## Проверка шаблона

Перед загрузкой убедитесь, что:

1. ✅ Файл имеет расширение `.xlsx` (не `.xls`)
2. ✅ Файл открывается в Excel без ошибок
3. ✅ Все плейсхолдеры написаны правильно: `${fieldName}`
4. ✅ Нет изображений, графиков, макросов
5. ✅ Структура таблиц правильная (см. документацию)

## Отладка

Если проблема не решается:

1. **Проверьте логи сервера:**
   - Ищите сообщения `[DocumentTemplate]`
   - Проверьте, есть ли ошибки при генерации

2. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Посмотрите на ошибки в Network или Console

3. **Проверьте шаблон:**
   - Откройте в Excel
   - Убедитесь, что нет ошибок
   - Проверьте структуру

## Альтернативное решение

Если проблема не решается, можно:
1. Использовать Word шаблоны (`.docx`) вместо Excel
2. Генерировать документы в PDF формате
3. Использовать другую библиотеку для генерации Excel (например, `exceljs`)

## Контрольный список

- [ ] Шаблон создан вручную в Excel
- [ ] Файл сохранен как `.xlsx`
- [ ] Файл открывается в Excel без ошибок
- [ ] Нет изображений, графиков, макросов
- [ ] Все плейсхолдеры написаны правильно
- [ ] Структура таблиц правильная
- [ ] Шаблон загружен в систему
- [ ] Шаблон установлен как шаблон по умолчанию
